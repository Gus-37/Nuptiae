// Reglas de seguridad para Firebase Storage
// Copiar y pegar en: Firebase Console → Storage → Reglas

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura pública de todas las imágenes
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true; // Para desarrollo - cambiar en producción
    }
    
    // Para producción, usar esto en lugar de lo anterior:
    /*
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Solo usuarios autenticados
    }
    */
  }
}
